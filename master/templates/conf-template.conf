{% if config.pop('nested', True) %}
{{ config.name }}:
  {% for arg, val in config.params.items() %}
  - {{ arg }}: {{ val }}
  {% endfor %}
{% else %}
{% for arg, val in config.items() %}
{{ arg }}: {{ val }}
{% endfor %}
{% endif %}